{
  "name": "frontend",
  "version": "0.1.0",
  "homepage": ".",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.14.1",
    "@testing-library/react": "^13.0.0",
    "@testing-library/user-event": "^13.2.1",
    "@vitejs/plugin-react": "^1.3.2",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "react-scripts": "5.0.1",
    "vite": "^2.9.12",
    "web-vitals": "^2.1.0"
  },
  "scripts": {
    "start": "node filechange ./src jsx js --nocomment && react-scripts start",
    "build": "node filechange ./src jsx js --nocomment && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "vitestart": "node filechange ./src js jsx --nocomment && vite",
    "vitebuild": "node filechange ./src js jsx --nocomment && vite build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {}
}
